<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Secure UI Design System</title>

<style>
:root {
  --primary-600: #2563eb;
  --primary-500: #3b82f6;
  --primary-100: #dbeafe;

  --neutral-900: #111827;
  --neutral-700: #374151;
  --neutral-400: #9ca3af;
  --neutral-200: #e5e7eb;
  --neutral-100: #f3f4f6;

  --success: #16a34a;
  --warning: #f59e0b;
  --error: #dc2626;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 16px;
  --space-4: 24px;

  --radius-sm: 6px;
  --radius-md: 10px;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  font-family: Inter, Arial, sans-serif;
  background: var(--neutral-100);
  color: var(--neutral-900);
}

header {
  background: white;
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--neutral-200);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

nav {
  background: white;
  border-bottom: 1px solid var(--neutral-200);
  padding: var(--space-2) var(--space-4);
  display: flex;
  gap: var(--space-3);
}

nav a {
  text-decoration: none;
  color: var(--neutral-700);
  font-weight: 500;
}

nav a:hover {
  color: var(--primary-600);
}

.container {
  max-width: 1100px;
  margin: auto;
  padding: var(--space-4);
}

.section {
  margin-bottom: var(--space-4);
}

.card {
  background: white;
  padding: var(--space-3);
  border-radius: var(--radius-md);
  border: 1px solid var(--neutral-200);
  margin-bottom: var(--space-3);
}

.button {
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-sm);
  border: none;
  cursor: pointer;
  font-weight: 500;
}

.button-primary {
  background: var(--primary-600);
  color: white;
}

.badge {
  padding: 3px 10px;
  border-radius: 20px;
  font-size: 12px;
  font-weight: 600;
}

.badge-success {
  background: #dcfce7;
  color: var(--success);
}

footer {
  text-align: center;
  padding: var(--space-3);
  color: var(--neutral-400);
}
</style>
</head>

<body>

<header>
  <h1>Secure UI Design System</h1>
  <div class="badge badge-success">● Secure</div>
</header>

<nav>
  <a href="#">Dashboard</a>
  <a href="#">Agenda</a>
  <a href="#">Email</a>
  <a href="#">Locker</a>
  <a href="#">Circle</a>
  <a href="#">Control Center</a>
</nav>

<div class="container">

  <div class="section">
    <h2>Design Principles</h2>
    <div class="card">
      Unified life management platform with modular and secure architecture.
    </div>
  </div>

  <div class="section">
    <h2>Components</h2>
    <div class="card">
      <button class="button button-primary">Primary Button</button>
    </div>
  </div>

</div>

<footer>
  Secure Unified System • v1.0
</footer>

</body>
</html>